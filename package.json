{
  "name": "monorepo",
  "version": "1.0.0",
  "description": "monorepo",
  "private": true,
  "workspaces": [
    "server",
    "app"
  ],
  "scripts": {
    "start:app": "cd app && yarn start",
    "start:server": "cd app && yarn start",
    "test": "cd app && yarn test"
  },
  "engines": {
    "node": "^20"
  }
}
